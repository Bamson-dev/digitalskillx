# DigitalSkillX V2 — Platform UX, Security & Reliability

Working notes for the polish initiative. Production enroll paths remain untouched (see `ENROLLMENT-LINK-PRODUCTION-RULES.md`).

## Tasks

1. Admin save UX — shared `useFormStateSaveUx` / `AdminSaveButton` + toasts
2. Course Editor — primary settings + curriculum; resources + YouTube under secondary/Advanced
3. Admin loading — `(panel)/loading.tsx` + skeleton components
4. Enrollment Engine — links-only (unchanged)
5. VideoProvider — architecture only (`lib/video-provider.ts` + docs)
6. Account Security — `account_sessions` + settings UI + soft login tracking
7. Course analytics — `lib/course-analytics.ts` + admin analytics page
